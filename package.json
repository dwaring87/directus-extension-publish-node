{
    "name": "@dwaring87/directus-extension-publish-local",
    "workspaces": [
        "./directus-module-publish-local",
        "./directus-endpoint-publish-local"
    ],
    "scripts": {
        "build": "npm run build --workspaces",
        "deploy": "default_dir=$(grep DEFAULT_DIRECTUS_EXTENSIONS_DIR .env | cut -d '=' -f 2) && DIRECTUS_EXTENSIONS_DIR=\"${DIRECTUS_EXTENSIONS_DIR:-$default_dir}\" npm run deploy --workspaces"
    }
}